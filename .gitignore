# ---- MATLAB ----
*.asv
*.m~
resources/
*.slxc
*.slprj
*.mlappinstall
*.mlpkginstall

# ---- Python ----
__pycache__/
*.py[cod]
.venv/
venv/
runs/
datasets/
*.egg-info/

# ---- AI 代理 / 构建工具运行时状态（非项目源码） ----
.reasonix/
.buildtool/

# ---- 生成的可视化图 / 中间产物 ----
viz/

# ---- 视觉模块：独立 git 仓库（自带 origin，参考不改），保持独立不纳入本仓库 ----
vision/D405_MarkerVision/

# ---- 编辑器 / 系统 ----
Thumbs.db
Desktop.ini
.DS_Store

# ---- Office 临时 / 锁文件 ----
~$*
~WRL*.tmp
$null

# ---- 大型可再生二进制（训练产物/示范数据；源码与训练脚本已入仓） ----
# 已从版本库 git rm --cached，工作区保留。如需重新交付模型快照，
# 可临时注释对应规则后再 git add -f
rl_pipeline/policy_*.mat
rl_pipeline/policy_*.npz
data/demonstrations_*/

# ---- 电控 dSPACE 工程构建产物（可再生，勿入库） ----
电控文件/**/Build/
电控文件/**/*.o86
电控文件/**/*.map
电控文件/**/*.mk
电控文件/**/*.part

# ---- AI 代理运行时记忆/状态（非项目源码，勿提交） ----
.workbuddy/
