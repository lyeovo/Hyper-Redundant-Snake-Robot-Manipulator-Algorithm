# 项目精简聚合分析

> 分析日期：2026-09-02 ｜ 方式：跨文件重复度扫描 + 函数引用图分析 + 资产体积盘点
> 结论：项目**不是"太大"，而是"同一份东西写了太多遍"**。可聚合点集中在 6 类，合计可消除约 **600+ 行重复代码、约 40MB 冗余资产**，且不损失任何现有能力。

---

## 0. 一句话总览

| 类别 | 当前 | 聚合后 | 收益 |
|---|---|---|---|
| 重复的取值 helper | 26 份 | 1 份 | ~130 行 |
| 距离函数双份实现 | 2 份（其中 43 行纯副本） | 1 份 | ~43 行 |
| RRT / RRT\* | 526 行（约半份重复） | 1 份 + 开关 | ~150 行 |
| 评估脚本 | 3 份同构 | 1 份 + 配置 | ~100 行 |
| TCP 帧读写 | 2 份相同定义 | 1 份 | ~8 行 |
| 示范数据目录 | 25 个（含 5 组并发生成分片） | 4~5 个 | 目录数 -80% |
| 策略模型快照 | 8 个 / 25MB | 2 个 / 6MB | -19MB |
| 死代码 | bcPolicy 全家桶等 | 清理 | — |
| 文档 | 8 HTML + pptx + pdf，1.04MB | 1 站 + 单一事实源 | 消除不同步 |

---

## 1. 代码重复（最高优先，零风险）

### 1.1 同一个 5 行 helper 被复制了 26 遍 ⭐ 最典型

全仓库扫描结果——**功能完全相同，只有名字不同**：

```
of(s,f,d)        × 15   method_momentum / method_sa / method_rrt / method_rrtstar
                        method_graph / method_prm / method_multilayer / method_rl
                        refineRandomGreedy / optimizeTraj / sampleTask2D
                        sampleObstacleScene / generateDemonstrations / evaluatePolicy
                        buildConnectivityGraph / validate_connectivity
getopt(...)      × 2    taskExecute / runTaskLoop
getopt2(s,f,d)   × 2    runTopLevel / handEyeEstimate2D
getf(s,f,d)      × 2    motorCmdPayload / ArmSimApp
gp(s,f,d)        × 1    taskToSegments
ifs(s,f,d)       × 1    taskToSegments   ← 与 gp 同文件内容完全一样
of(s,f,d)        × 2    scripts/trainRLPolicy / scripts/verifyPolicy
```

`taskToSegments.m` 里 `gp` 和 `ifs` **两个函数体逐字相同**，只是名字不同——最直观的例子。

**建议**：在 `ArmSimulator2D/` 建 `optget(s, field, default)`（或复用 MATLAB 自带的 `inputParser`），26 处全部删掉本地定义。这是纯机械替换，零行为变化，可先做这一项建立信心。

### 1.2 `obsDistAll.m` 是 `obsDistGradAll.m` 的删梯度副本

`obsDistAll.m` 第 3 行注释自己写着：

> `%   与 obsDistGradAll 的 g_all 输出完全一致（遍历顺序相同）`

逐行 diff 确认：43 行里除了去掉 `dg_all` 和 `Jp` 计算，逻辑一模一样。

**建议**：`obsDistAll` 改成薄封装
```matlab
function g_all = obsDistAll(model, q)
    [g_all, ~] = obsDistGradAll(model, q);
end
```
保留函数名（调用点很多），实现从 43 行变 3 行，且**彻底消除"两处距离计算逻辑可能走偏"的风险**——这对碰撞检查的正确性是实质性改进，不只是省代码。

### 1.3 RRT 与 RRT\* 约有一半是复制

| 文件 | 行数 |
|---|---|
| `method_rrt.m` | 210 |
| `method_rrtstar.m` | 316 |
| 忽略空白/大小写后的差异 | 164 行（rrtstar 独有） |

RRT\* 相对 RRT 的本质差异只有三处：**near 邻域搜索 → chooseParent → rewire**。其余（采样、碰撞检测、最近邻、树扩展、目标判定、精修）完全同源。

**建议**：提取 `rrt_core(model, q0, target, opts, 'star', true/false)`，两个入口函数各自保留（README 明确说 `rrt` 是留作对比基线的，语义要保留），但内部共用一份。**风险提示**：这是本次唯一涉及算法行为的重构，必须先在修复后的测试上跑通（见 §5 前置条件）。

### 1.4 三份评估脚本是同一份的三份复制

| 文件 | 行数 | 与 eval_c1 的差异行数 |
|---|---|---|
| `scripts/eval_c1_compare.m` | 50 | — |
| `scripts/eval_c3_compare.m` | 50 | 50（几乎全改，只因多了一组难度 3 任务） |
| `scripts/eval_v2_compare.m` | 50 | 26 |

三者**主循环逻辑完全一致**（`sampleTask2D` → `cvaePolicyDeploy` → 统计成功率/直出率/末端精度），差异仅是：`rng` 种子（11 / 11+23 / 5）、任务数（40 / 40+20 / 150）、模型文件列表。

**建议**：合成 `scripts/eval_policy_compare.m(model_files, task_specs, rng_seed)`，原三个文件保留为 3 行配置入口（保住可重现性——实验脚本的调用方式本身就是结果的一部分）。

### 1.5 `javaWriteFrame` 定义两遍

`tcpMotionServer.m:91` 与 `tcpStateStreamServer.m:39` **逐字相同**。抽到 `tcpEncodeFrame.m` 旁边即可。

---

## 2. 数据资产（体积最大，收益最直接）

### 2.1 25 个示范目录中，5 组是并发生成分片

| 分片组 | 目录数 | 单目录体积 | 说明 |
|---|---|---|---|
| `demonstrations_c1_1` ~ `c1_6` | 6 | 各 288K | 同规格，明显是 6 路并发生成 |
| `demonstrations_c1a` ~ `c1d` | 4 | 各 44K | 同上 |
| `demonstrations_c3_1` ~ `c3_6` | 6 | 各 64K | 同上 |
| `demonstrations_opt1` ~ `opt3` | 3 | 各 400K | 同上 |
| `demonstrations_var2` ~ `var4` | 3 | 各 17~29K | 同上 |
| 独立集 | 3 | `mix` 8.1M / `n6` 8.0M / — | 已合并的成品 |

**关键证据**：`rl_pipeline/merge_demos.py` 的 docstring 就是
> `"""合并多个示范分片目录 → 单一训练目录（shuffle + 统一重命名 + 重写 manifest）"""`

即：合并工具写好了，分片也合并过了，**但原始分片没清理**。

**建议**：跑 `merge_demos.py` 把 5 组分片各合并成一个（`demonstrations_c1_merged` 等），原分片移到 `archive/demonstrations_shards/` 或直接删除（它们可从合并结果 + manifest 追溯）。

### 2.2 8 个策略模型，7 个已被取代

```
policy_cvae_c1_ft.mat   3.1MB   ← 当前默认（文档 §4.5 标记为"默认"）
policy_cvae_v2.mat      3.1MB   云训练产物
policy_cvae_v3.mat      3.1MB   云训练产物
policy_cvae_c1.mat      3.1MB   微调前
policy_cvae_opt.mat     3.1MB   升级版示范训练
policy_cvae_mix.mat     3.1MB   混合集
policy_cvae_n6.mat      3.1MB   首版
policy_bc_n6.mat        1.5MB   BC 路线（已废弃）
policy_bc_n6.npz        1.6MB   BC 路线（已废弃）
                        ────
                        25MB
```

**建议**：git 里只留 `c1_ft`（默认）+ 视需要留 `v3`（最新）；其余移出版本控制（保留在工作区或归档目录）。`.gitignore` 里已经预留了注释掉的 `rl_pipeline/policy_*.mat`——启用它，需要交付哪个再显式 `git add -f`。

---

## 3. 死代码

引用图分析（排除 `archive/` 后统计各函数被**其他文件**引用的次数）：

| 对象 | 状态 | 判断 |
|---|---|---|
| `ArmSimulator2D/bcPolicyFromFile.m` | **零外部引用**（只在自身文件出现） | 真死代码。对应 BC 行为克隆路线，已被 CVAE 取代 |
| `rl_pipeline/policy_bc_n6.mat` / `.npz` | 仅被 `bcPolicyFromFile` 引用 | 同上，连带清理 |
| `rl_pipeline/train_policy.py` | BC 路线训练脚本 | 同上（CVAE 是 `train_cvae.py`） |
| `archive/`（341K） | 已归档 | 含 `runLArmIK_2D.m` **105KB**、`PlanarDrawApp.m` 63KB、`TopLevelSystem.m` 29KB 等，保持归档即可 |
| `solveIK.m` | 引用 0 次 | **不要删**——README 首屏宣传的对外快捷 API，属公共接口 |
| `buildfile.m` | 引用 0 次 | **不要删**，见 §5，它是现成的 CI 抓手 |
| `method_rl.m` / `rlRollout.m` | 被 GUI 和 `simulateMotion` 引用 | 活着，但与 CVAE 是**两条并行学习路线**（OpenAI-ES residual vs CVAE 多模态）。若确认 CVAE 是主线，可考虑后续收敛，但当前不动 |

**注意**：`method_rl`（OpenAI-ES）与 `cvae` 并存本身是有价值的对比，不建议现在删；要删的是已经连入口都没有的 BC 路线。

---

## 4. 目录与文档归属

### 4.1 文件放错了地方

- `tcpSendMotionCli.m` 在**根目录**，其余 9 个 `tcp*.m` 全在 `ArmSimulator2D/` —— CLI 包装器应移进同目录（或统一提到根目录，二选一，别劈开）
- `scripts/` 下混着 3 个 `test_*.m`（`test_handeye_diag` / `test_handeye_synth` / `test_toplevel_diag`），而正式测试在 `test/`（12 个）——诊断脚本归 `scripts/` 其实合理，但要与 `test/` 划清：进 `test_all` 的放 `test/`，手动诊断的放 `scripts/`，并在各自 README 注明

### 4.2 文档：问题不是"太多"，是"没有单一来源"

8 个 HTML + pptx + pdf 共 **1.04MB**，两两内容重合度（Jaccard）只有 **0.08~0.17**——它们讲的是**不同切面**（项目介绍 / 架构总览 / 求解策略 / 学习管线 / 三维化 / 运行机制 / 算法理论 / 电控接口），**内容本身不重复，不该硬并成一份**。

真正的问题是：

1. **同一事实多处表述，改一处漏一处**。这正是上次分析发现的文档漂移（README 与架构文档仍在描述旧任务类型，与 9/2 重构后的代码脱节）的成因。
2. **`电控集成接口说明.html` 619KB，正文只有 458 词——98.4% 是 Archify 生成器的 JS/CSS 脚手架**。这是纯粹的冗余体积。
3. 8 个文件之间**没有互相导航链接**，使用者得记住 8 个文件名。

**建议**：
- 建一个 `docs/index.html` 做导航壳（8 个切面各一个入口，互相可跳转），**不合并内容**
- 把"事实性数据"（方法列表 / 参数默认值 / 错误码 / 接口字段）收敛到 `文档/系统架构与接口总览.md` 作为**唯一事实来源**，HTML 只做呈现、不重复定义事实
- `电控集成接口说明.html` 若需长期保留，用 Archify 导出为纯 SVG/PNG（几十 KB），HTML 脚手架不必入库

---

## 5. 前置条件：先修测试，再动刀

`buildfile.m` 已经定义了完整的构建任务，但**没接 CI**——这正好是最省力的抓手：

```matlab
function plan = buildfile
plan("clean") = CleanTask;
plan("check")  = CodeIssuesTask;   % 静态代码检查
plan("test")   = TestTask;         % 单元测试
plan.DefaultTasks = ["check" "test"];
end
```

跑法：`matlab -batch "buildtool"`（或 `buildtool check test`）。

**但必须先修掉上次发现的两个假绿，否则这轮重构没有任何安全网**：

1. `test_all.m:15` 无条件打印"全部通过" —— 改为聚合各测试失败数，非 0 则 `error()`
2. `test_gradcheck` 场景 B（圆障碍）采样全被 margin 过滤 → 返回 NaN → `NaN > 1e-5` 为 false → 假通过。**圆障碍的解析梯度从未被真正验证过**
3. `test_gui.m:18` 的 `== 5`（任务类型数）已过期，现在 GUI 是 11 种

这三处修完，`buildtool test` 才能当真。

---

## 6. 执行顺序（按收益/风险比）

| 序 | 动作 | 收益 | 风险 | 前置 |
|---|---|---|---|---|
| 1 | 修 3 处测试假绿，接上 `buildtool` | 建立安全网 | 无 | — |
| 2 | 26 份 helper → 1 份 `optget` | -130 行 | 零（机械替换） | 1 |
| 3 | `obsDistAll` → 薄封装 `obsDistGradAll` | -43 行，消除双份距离逻辑 | 极低 | 1 |
| 4 | `javaWriteFrame` 去重 | -8 行 | 零 | 1 |
| 5 | 示范分片合并 + 清理 | 目录 25→5 | 低（有 merge 工具 + manifest） | 无 |
| 6 | 策略模型移出 git（留 c1_ft + v3） | -19MB 仓库 | 低（.gitignore 已预留） | 无 |
| 7 | 删 BC 路线（`bcPolicyFromFile` / `policy_bc_*` / `train_policy.py`） | 清死代码 | 低（零引用） | 1 |
| 8 | `tcpSendMotionCli.m` 归位 | 目录一致 | 零 | 无 |
| 9 | 3 个 eval 脚本合 1 | -100 行 | 低（保留薄入口） | 1 |
| 10 | 建 `docs/index.html` 导航 + 单一事实源 | 消除文档漂移 | 中（要改多处） | 无 |
| 11 | RRT/RRT\* 提取 `rrt_core` | -150 行 | **中（动算法）** | 1 + 专项验证 |
| 12 | `电控集成接口说明.html` 转静态图 | -600KB | 无 | 无 |

**第 1 项是前提，不是可选项。** 没有可信的测试，第 11 项（唯一涉及算法行为的重构）不该做。

---

## 7. 不该动的地方

- **`method_rl` / `cvae` 双学习路线并存** —— 是有价值的对比，不是重复
- **8 个 HTML 文档的内容** —— 重合度只有 0.08~0.17，是不同切面，硬并会丢失信息
- **`solveIK.m` / `buildfile.m`** —— 零引用但分别是对外 API 和 CI 入口
- **`archive/`** —— 已归档的死代码有迁移对照价值（README 明确说 `PlanarDrawApp` 保留作对照）
- **`电控文件/`** —— dSPACE 构建产物虽然该 gitignore，但那是**源码工程**，不能精简，只能管好版本控制
