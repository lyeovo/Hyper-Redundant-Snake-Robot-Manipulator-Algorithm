# 项目分析报告 — PlanarArm 平面超冗余蛇形机械臂

> 分析日期：2026-09-02 ｜ 分析方式：静态走查 + MATLAB R2026a 实机回归测试 + 跨模块契约实测
> 结论一句话：**算法内核成熟、工程化扎实，但当前最大风险已从"算法能不能解"转移到"模块之间对不齐"——视觉与规划的接口契约已经断裂，而回归测试无法发现它。**

---

## 1. 项目是什么

一个**中间环节模块**：承接上游视觉的目标识别结果，输出下游电控可加载的电机指令序列。

```
视觉 (SpaceSnakeVisionUI, D405+YOLO)      [本模块 MATLAB]                     电控 (dSPACE)
  检测目标 → TaskCommand JSON  ──▶  任务桥 → IK/避障求解 → motorCmd  ──▶  离线加载 + 实时插值执行
  data/outbox                        ArmSimulator2D 内核                    q_seq/t_seq/gripper_seq
                                     ← TaskStatus 回写 (data/inbox)
```

- 机械臂：**6 关节 × 1043.93 mm = 6.264 m** 平面超冗余蛇形臂，地面水平面夹取/放置
- 末端：夹爪（单电机开合）+ 视觉相机
- 电控：dSPACE（Simulink R2018b 生成 C 代码，步进电机 Dir/Pulse 驱动）

---

## 2. 资产盘点（实测）

| 部分 | 规模 | 状态 |
|---|---|---|
| `ArmSimulator2D/` | 70 个 .m，4976 行 | 核心，纯函数，分层清晰 |
| 顶层接口 | 10 个 .m（taskExecute / runTaskLoop / exportMotorCmd / ArmSimApp 68KB 等） | 可用 |
| `test/` | 12 个文件，test_all 调度 10 项 | **有假绿，见 §4.2** |
| `rl_pipeline/` | 3 个 .py + 8 个策略快照（各 ~3.2MB） | CVAE 管线跑通 |
| `data/` | 212 个 .mat 示范数据（已入仓，.git 47MB） | 25 个数据集 |
| `文档/` | 9 md + 8 docx（实现方案 60KB 为主设计） | 质量高，但**已滞后于代码** |
| `电控文件/` | dSPACE 工程，378 文件，3 个 Application | **仅单电机/夹爪点动测试** |
| `vision/` | 独立 git 仓库，122 个 .py | 参考不改 |
| 可视化文档 | 7 个 HTML + 总图.pptx + 算法总图.pdf | Archify 生成 |

**近期方向（8/27–9/2，未提交）**：工作重心明显从"算法"转向"集成"——
LGM 多层图引导法（`method_multilayer` + `buildConnectivityGraph`）、TCP 锁步下发栈（`tcpSendMotion`/`tcpMotionServer`/`tcpStateStreamServer`，帧格式 `[4字节大端长度][JSON]`）、8 通道电机模型（`motorConfig`：6 关节 + 夹爪朝向 + 夹爪开合）、抽稀误差评估（`tcpDecimateCheck`）。**方向正确，但集成验证没跟上。**

---

## 3. 实测结果（本地 MATLAB R2026a Update 3 跑出来的，不是文档抄的）

```
>> test_all                    耗时 62s
   test_obstacle_geometry  0 失败
   test_gradcheck          A 无障碍 3.577e-08 ｜ B 圆障碍 NaN ｜ C 矩形 9.928e-09 ｜ 雅可比 2.637e-09
   test_createModel / test_simulateMotion / test_errorModel / test_methodRl   0 失败
   test_taskBridge / test_taskLoop                                            0 失败
   test_gui                失败断言: 3 ｜ 1 项失败   ← 未中断
   test_topLevel           3 通过（手眼收敛于帧 4，目标末误差 0.0114，任务末端误差 0.0099）
   =========== 全部通过 ===========        ← 与上一行矛盾
```

关键算法指标成立：`auto` 链在 6 关节 1.04m 臂上，无障碍快路径 0.08s（momentum，误差 <1e-4）、障碍场景 0.34s（RRT*，误差 0.0146m）。手眼在线估计 + Kalman 的顶层闭环可在 4 帧收敛到 1cm 级。**算法内核是可信的。**

---

## 4. 关键风险（按阻塞程度排序）

### P0-1 ｜ 视觉 ↔ 规划：`command_type` 枚举交集为空 —— 链路完全不通

实测比对（`vision/.../src/ui/command_panel.py` vs `taskToSegments.m`）：

| 侧 | 指令类型 |
|---|---|
| 视觉下发 | `move_near_target` `pick_target` `pick_and_place` `dock_to_interface` `home`（+`emergency_stop` `cancel_task`） |
| MATLAB 支持 | `move_to` `move_along` `move_for_pick` `move_for_place` `rotate` `rotate_arm` `facing_arm` `pick` `place` `withdraw` `reset` |
| **交集** | **空** |

全仓库 grep 确认 MATLAB 侧对旧类型**零兼容**。视觉 UI 实际产出的那条指令（`data/outbox/CMD-20260902-88945.json`，`command_type: move_near_target`）进去就抛 `未知 command_type`。
**原因**：9/2 重构把任务类型从"任务语义"改成了"原子动作语义"，但没同步改视觉侧，也没改测试里的 mock（测试用的是新类型，所以测不出来）。

**影响**：任意一条真实视觉指令都会让 `taskExecute` 直接失败。

### P0-2 ｜ `pose_camera` 为 null 时直接崩溃

同一条真实指令里 `selected_target.pose_camera: null`（`status: BEARING_ONLY`，只有方位射线、没有深度位姿）。实测：

```
>> taskToSegments(cmd, [], 0.15)
*** 失败: 无法解析位置字段          ← projectTo2D 抛错
```

`taskToSegments.m:28` 无条件 `projectTo2D(cmd.selected_target.pose_camera)`，没有 null 分支，也没有 BEARING_ONLY 的降级路径（用 bearing 射线求交地面平面）。

### P0-3 ｜ `motion_params` 键名不匹配 —— 静默错误，比崩溃更危险

| 视觉下发键 | MATLAB 读取键 |
|---|---|
| `approach_distance_m` (0.05) | `d` ← 读不到，**静默回退默认 0.15** |
| `speed_mode` / `gripper_mode` / `stop_if_target_lost` | 完全忽略 |
| （无） | `theta` / `alpha` / `n` ← 永远取默认 0.5 / 1 |

视觉要求接近距离 0.05m，实际执行 0.15m；`rotate_arm`/`facing_arm` 依赖的 `alpha`/`n` 永远取默认值。**这些不会报错，只会让动作做错。**

### P0-4 ｜ 工作空间尺度差一个数量级

- 机械臂全长 **6.264 m**，CVAE 栅格 EXTENT = x[-5,9] y[-7,7]（14×14 m）
- 视觉侧 `safety.yaml: max_reach_m: 0.8`，destination 实测 **(0.35, 0.10) m**

IK 在 0.35m 目标上确实能解（momentum 0.08s 成功），但那意味着 6 米长的臂要极度折叠去够 35cm 外的点。两边对"工作空间有多大"的根本假设不一致，必须先对齐再谈精度。

### P1-5 ｜ 回归测试假绿（两处，且互相掩盖）

1. **`test_gradcheck` 场景 B（圆障碍）实际一次都没校验**。20 个随机采样点因 `margin` 过滤全部 `continue`，`errs` 为空 → `e = NaN` → `if eB > 1e-5` 在 NaN 上为 false → 打印"通过"。**圆障碍的解析梯度从未被验证过。**
2. **`test_all.m` 无条件打印"全部通过"**（第 15 行无失败计数聚合）。`test_gui` 报了"1 项失败"仍输出全部通过。
3. `test_gui` 第 18 行 `ok(3) = numel(...) == 5`（"5 种任务类型"）——现在 GUI 是 11 种，断言过期。

**后果**：CI 信号不可信。P0-1 这类契约断裂，现有测试一条都抓不到。

### P1-6 ｜ 无自碰撞检测（物理可行性缺口）

`grep -i self` 在 `ArmSimulator2D/` **零命中**；`obsDistAll.m` 只遍历「障碍物 × 臂段」。6 关节超冗余臂在 0.35m 近距离目标下必然极度折叠，规划出的解很可能是自穿透的。当前 `error_code=9`（碰撞）只覆盖环境障碍。

### P1-7 ｜ TCP 协议是单边的

MATLAB 侧 9/1–9/2 新建了一整套协议（`HELLO/MOVE/MOVE_ACK/STATE/DONE/PING/PONG`，锁步下发，步进量化 40000 步/圈 = 0.009°）。但 dSPACE 侧：

- `grep -rl "socket|json|trajectory|q_seq"` 在 378 个电控源文件中 **零命中**
- 现有 3 个 Application 只是：`motiontest0829`（单路 Dir+/Dir-/Pulse 点动，±5V 饱和）、`gptest0830` / `gp2test0830_2`（夹爪 PWM）

即：**协议目前只在 MATLAB 自己跟自己对话**（`tcpMotionServer` 是参考实现/模拟器）。dSPACE 侧需要先落一个"TCP 收帧 → JSON 解析 → 按 rel_deg 发脉冲"的模型。

### P2-8 ｜ 仓库卫生

- `电控文件/`（378 文件，含 60 个 .o86 ELF 目标文件、.map/.rta/.trc 构建产物）**未加入 .gitignore**，一旦 `git add .` 就会把 dSPACE 编译中间产物灌进仓库
- 7 个 3.2MB 的策略快照（v2/v3/mix/c1/n6/opt/c1_ft）全部入仓，.git 已 47MB；其中大部分已被 `c1_ft` 取代
- 212 个 .mat 示范数据入仓（.gitignore 里 `demonstrations_*/` 被注释掉了）

### P2-9 ｜ 文档漂移

README 与架构文档仍在描述旧的任务类型集（`move_near_target/pick_target/pick_and_place/dock_to_interface/home`），与 9/2 重构后的 `taskToSegments` 不一致；README 提到的 `cvae` 方法在 `simulateMotion` 的方法分派里其实不存在（走的是独立的 `cvaePolicyDeploy`）。新同学按 README 上手会踩坑。

---

## 5. 建议动作（按依赖顺序）

**第一步：修测试，让它能报警（半天）**
1. `test_all.m` 改为聚合各测试返回的失败数，非 0 则 `error()` 退出（CI 才能红灯）
2. `test_gradcheck` 场景 B：`isempty(errs)` 时改为 `error()` 而非返回 NaN；或放宽 margin 保证至少 N 个有效采样
3. `test_gui` 第 18 行 `== 5` 改成 `== 11`

**第二步：对齐契约（1–2 天，这是当前主线）**
4. 在 `taskToSegments` 或 `taskExecute` 前加**指令类型适配层**：`move_near_target → move_to`、`pick_target → [move_for_pick, pick, withdraw]`、`pick_and_place → [...]`、`dock_to_interface → move_for_place`、`home → reset`。**建议放在 MATLAB 侧**（视觉侧是独立仓库且已交付）
5. 加**契约守卫**：`taskExecute` 入口对 `command_type` / `pose_camera` / `motion_params` 键名做校验，缺失或不认识立刻 `REJECTED` + 明确 message，而不是抛栈或静默取默认
6. `projectTo2D` 支持 `pose_camera == null` 的 BEARING_ONLY 降级（用 `bearing.ray_camera` 与地面平面求交）
7. `motion_params` 键名统一为视觉侧口径（`approach_distance_m`），或显式映射 + 未识别键告警

**第三步：对齐物理假设（需外部输入）**
8. 与机械/电控确认：**工作空间到底是 0.8m 还是 6.26m 半径**——这决定 `safety.max_reach_m`、destination 配置、CVAE 的 EXTENT/GRID，也决定要不要重训策略（现有示范全是 0.5m/1.04m 口径）
9. 加自碰撞项到 `armValue`/`obsDistAll`（`segSegDistGrad.m` 已有段-段距离与梯度，可直接复用，只需跳过相邻段）

**第四步：闭环（依赖 8、9）**
10. dSPACE 侧先落一个最小 TCP 接收模型（收 `MOVE` → 按 `rel_deg` 发 Dir/Pulse 脉冲 → 回 `MOVE_ACK`），再用 `tcpMotionServer` 的契约做对拷验证
11. 建端到端验收脚本：真实 outbox JSON → runTaskLoop → 求解 → motorCmd →（mock 电控）→ 状态回传，统计成功率/末端精度/节拍/碰撞

**仓库维护（随手做）**
12. `.gitignore` 加 `电控文件/**/Build/`、`*.o86`、`*.map`、`*.rta`、`*.trc`；把历史策略快照移出 git（保留 `c1_ft` 一个）或上 Git LFS

---

## 6. 总体判断

| 维度 | 评价 |
|---|---|
| 算法内核 | **强**。统一价值函数 + 解析梯度（机器精度校验）+ 9 种方法 + auto 调度链 + LGM 多层图引导，实测收敛快、误差小 |
| 代码工程化 | **好**。纯函数分层、单一事实来源（`modelDefaults`）、错误码体系完整、注释密度高 |
| 文档 | **质量高但滞后**。设计文档（60KB 实现方案）远超一般水平，但与 9/2 重构后的代码脱节 |
| 集成 | **弱**。契约断裂（P0-1~4）+ 测试假绿（P1-5）叠加，导致"看起来全绿，实际一条真实指令都跑不通" |
| 测试有效性 | **不合格**，是当前最需要优先修的部分——因为它掩盖了上面所有问题 |

**最优先的一件事**：先修测试（第 1–3 项）。不修测试，后面所有集成修复都无法被验证，也无法防止回归。
